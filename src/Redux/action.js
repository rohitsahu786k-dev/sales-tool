import { setActiveDesk, setActiveColor, setActiveLongDescription } from './desk-list/desk-slice'
import { setWheelIndex, setDisplayImage } from './product-info/product-slice'
import { updateChildItem, setNext, resetChild, updateDisplayImage, setDisableNext } from './wheel-item/wheel-item-slice';

export {
    setActiveDesk,
    setWheelIndex,
    setActiveColor,
    setActiveLongDescription,
    updateChildItem,
    setNext,
    resetChild,
    updateDisplayImage,
    setDisableNext,
    setDisplayImage,
}